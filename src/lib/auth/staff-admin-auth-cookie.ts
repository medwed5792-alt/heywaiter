/** HttpOnly cookie: Firebase ID token (подпись Google). Роль только из Firestore global_users — см. middleware. */
export const HEYWAITER_STAFF_ADMIN_AUTH_COOKIE = "heywaiter_staff_firebase_session";
